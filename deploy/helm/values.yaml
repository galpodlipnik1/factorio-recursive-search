image:
  repository: ghcr.io/galpodlipnik1/rbf-api
  tag: "ff3083f"

imagePullSecrets:
  - ghcr-pull

service:
  port: 8080

ingress:
  enabled: true
  className: nginx
  host: rbf-api.galpodlipnik.com
  proxyBodySize: 100m
