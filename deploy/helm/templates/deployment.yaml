apiVersion: apps/v1
kind: Deployment
metadata:
  name: rbf-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rbf-api
  template:
    metadata:
      labels:
        app: rbf-api
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . | quote }}
        {{- end }}
      {{- end }}
      containers:
        - name: rbf-api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: Always
          env:
            - name: RBF_INSTALL_RATE_LIMIT_PER_MINUTE
              value: {{ .Values.rateLimit.install.requestsPerMinute | quote }}
            - name: RBF_INSTALL_RATE_LIMIT_BURST
              value: {{ .Values.rateLimit.install.burst | quote }}
            - name: RBF_INDEX_RATE_LIMIT_PER_MINUTE
              value: {{ .Values.rateLimit.index.requestsPerMinute | quote }}
            - name: RBF_INDEX_RATE_LIMIT_BURST
              value: {{ .Values.rateLimit.index.burst | quote }}
          ports:
            - containerPort: {{ .Values.service.port }}
              name: http
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
